# Which sound provider the server talks to.
#   mixcloud → the real Mixcloud API (default)
#   mock     → offline demo data, no external calls (useful for demos, tests and flaky networks)
SOUND_PROVIDER=mixcloud
